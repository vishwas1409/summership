PORT=3000
NODE_ENV=production

# --- Local / Hostinger (use DB_* variables) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=hackathon_selection

# --- Railway (auto-set when MySQL database is linked — do NOT use localhost/root) ---
# MYSQLHOST=
# MYSQLPORT=
# MYSQLUSER=
# MYSQLPASSWORD=
# MYSQLDATABASE=

SESSION_SECRET=super_secret_session_key_here
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_to_a_secure_password
